namespace Contrive.Common
{
  /// <summary>
  ///   Interface for listeners. By convention, listeners are registered with the container as singletons and are instantiated during application startup.
  /// </summary>
  public interface IListener {}
}